import { commonClasses } from "@/lib/utils";
import React from "react";

type Props = {};

const Skills = (props: Props) => {
  return <section className={`${commonClasses.rootSection}`}>Skills</section>;
};

export default Skills;
